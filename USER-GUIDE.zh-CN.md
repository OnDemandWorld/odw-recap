# ODW Recap 使用指南（写给非技术人员）

> 本指南假设您**不懂编程**，只需要会安装普通软件、会点击按钮即可照着做。
> 所有界面按钮名称均以应用内英文原文为准（如 **Import**、**Process Meeting**）。

---

## 1. Recap 是什么？一句话版本

**Recap 是一个安装在您自己电脑上的"会议录音整理助手"**：把会议录音导入进来，它能帮您
转成文字（转写）、提炼出摘要、待办事项（Action Items）和会议决定（Decisions）。

它最大的特点是**保护隐私**：

- 录音文件保存在您自己的电脑里，不会自动上传到任何云；
- 除非您主动在设置里填了云端服务的钥匙（API Key）并选择使用云端服务，
  否则什么数据都不会离开您的电脑。

---

## 2. 开始之前，您需要准备什么

| 需要的东西 | 说明 | 没有会怎样 |
|---|---|---|
| 一台 Mac 或 Windows 电脑 | 用于安装 Recap 应用 | — |
| 会议录音文件 | 支持 M4A、WAV、MP3、OGG、WebM、FLAC 格式（手机录音、腾讯会议/Zoom 导出一般都支持） | 无法整理会议 |
| （可选）OpenAI 或 Deepgram 的 API Key | 用于把录音变成文字，相当于"云端服务的大门钥匙"，需要到对应官网注册申请 | 无法转写和生成摘要（本地转写引擎暂未开放，见 FAQ） |

**API Key 从哪里来？（由您或公司 IT 操作）**

1. 打开 OpenAI（platform.openai.com）或 Deepgram（console.deepgram.com）官网；
2. 注册账号并登录控制台；
3. 找到 "API Keys" 页面，点击创建，复制以 `sk-` 开头的一长串字符；
4. 注意：这类服务通常按用量收费，费率见对应官网。

> 提示：API Key 就像银行卡密码，**不要发给任何人、不要发到群里**。

---

## 3. 安装与启动

### 普通用户（已拿到安装包）

1. 向您的 IT 或项目组获取 Recap 安装包（macOS 为 `.dmg`，Windows 为 `.msi`）；
2. 双击安装包，把 Recap 图标拖进"应用程序"文件夹（Windows 一路点"下一步"）；
3. 在启动台/开始菜单中点击 **ODW Recap** 打开。

### 第一次打开

- 窗口打开后，您会看到四个标签页：**Meeting Library**（会议库）、**Import**（导入）、
  **Settings**（设置），以及会议详情页。
- 底部有一条状态栏，会随时告诉您"现在发生了什么"——看不懂界面时，先看它。

---

## 4. 认识主界面

```
┌──────────────────────────────────────────────┐
│  [ODW.AI 标志]  Recap   Meeting Intelligence │  ← 顶栏
│  [Meeting Library] [Import] [Settings]       │  ← 三个标签页
├──────────────────────────────────────────────┤
│                                              │
│              当前标签页的内容                  │
│                                              │
├──────────────────────────────────────────────┤
│  Ready                                       │  ← 状态栏（重要提示都出现在这里）
└──────────────────────────────────────────────┘
```

- **Meeting Library**：所有已导入的会议列表，每条显示标题、状态和时长。
  会议状态有四种：**Imported**（已导入，还没处理）、**Processing**（处理中）、
  **Ready**（整理完成，可查看结果）、**Failed**（失败，一般是没配 API Key）。
- **Import**：导入录音文件的地方。
- **Settings**：填 API Key、选服务商、开启"监听文件夹"和"手机上传"。

---

## 5. 第一次整理会议：完整走一遍

### 第 1 步：填入 API Key（只需要做一次）

1. 点击 **Settings** 标签；
2. 找到 **API Keys** 区域，在 **OpenAI API Key** 或 **Deepgram API Key**
   输入框中粘贴您的 Key；
3. 点击 **Save API Keys**。看到输入框旁边出现绿色的 **Saved ✓** 就成功了；
4. 顺便在同一页面把 **STT Provider**（转写服务商）选成您填了 Key 的那家
   （例如填了 OpenAI 的 Key 就选 **OpenAI Whisper API**）。

> API Key 在保存后会被**加密存放在您自己的电脑里**，连 Recap 界面都不会再显示它。

### 第 2 步：导入录音

三种方式任选其一：

- **方式 A（最简单）**：点 **Import** 标签 → 点 **Choose Audio File** → 选中录音文件 → 打开；
- **方式 B（拖拽）**：直接把录音文件从文件夹**拖进 Recap 窗口**；
- **方式 C（自动）**：在 **Settings** 里开启"监听文件夹"（见第 6 节），
  以后录音文件一出现就自动导入。

导入成功后，应用会自动跳到这个会议的详情页，状态栏显示
"Imported. Meeting … is ready to process."

### 第 3 步：处理（转写 + 摘要）

1. 在会议详情页点击 **Process Meeting** 按钮；
2. 状态栏会提示"正在转写和摘要，可能需要一段时间"（取决于录音时长，通常几分钟内）；
3. 完成后状态徽章变成 **Ready**，页面自动刷新出结果。

**如果变成 Failed**：把状态栏最后一行错误信息看一眼——十有八九是
"没有配 API Key"或"Key 不对"，按第 1 步重新配置后再点 **Process Meeting** 即可。

### 第 4 步：查看结果

详情页从上到下分五块：

| 区块 | 内容 | 用途 |
|---|---|---|
| Meeting Information | 状态、时长、文件大小、创建时间 | 快速确认 |
| Summary | 整场会议的摘要 | 快速回顾会议讲了什么 |
| Action Items | 会议中提到的待办事项列表 | 会后跟进任务 |
| Decisions | 会议中确定的决定列表 | 留档备查 |
| Transcript | 逐段带时间点的完整文字记录 | 核对细节、检索原文 |

---

## 6. 让 Recap 更省事：两个自动化功能（Settings 里）

### 监听文件夹（Watch Folder）——"文件一来就自动导入"

适合每周例会都录到同一个文件夹的情况。

1. **Settings** → **Audio Input** → 点 **Choose Folder…**；
2. 选择您的录音文件夹（例如"下载/会议录音"）；
3. 状态栏显示 "Watching folder: …" 即已生效。此后，**任何新出现的录音文件
   会在几秒内自动出现在会议库里**；
4. 不想用了点 **Stop**。

### 手机上传（HTTP Upload Server）——"手机录音直接传进电脑"

适合您用手机录音、想传到电脑上的场景。**要求手机和电脑连同一个 Wi-Fi。**

1. **Settings** → **Audio Input** → 勾选 **HTTP Upload Server** 的 **Enable**；
2. 页面会显示一行上传网址，形如
   `http://192.168.1.5:8765/upload/audio?token=xxxx`（每次开启都会变化）；
3. 在手机上打开这个网址（可让电脑上出现二维码的工具帮忙，或手动输入），
   选择录音文件上传；
4. 传完回到 Recap，新会议会出现在会议库里；
5. 用完记得**取消勾选**关闭服务。

> 安全说明：上传网址里带有一串一次性令牌（token），只有拿到完整网址的设备
> 才能上传，局域网里的其他设备无法乱传文件。

---

## 7. 删除会议

在会议详情页点 **Delete**，确认即可。该会议的转写、摘要、待办都会一并删除，
原始音频文件不受影响（仍在您的电脑里）。

---

## 8. 常见问题（FAQ）

**Q1：我的录音和转写内容会被上传到网上吗？**
只有两种情况会用到网络：① 您配置了云端转写/摘要服务（OpenAI/Deepgram/Anthropic），
处理时会把音频或文字发给**您选择的那家**服务商；② 您主动使用团队服务器同步功能。
除此之外，一切数据只保存在您电脑的 `~/RecapData` 文件夹里。

**Q2：点 Process Meeting 报错 "Local transcription (whisper.cpp) is not available yet"？**
本地转写引擎还在开发中。目前请按第 5 节第 1 步配置 OpenAI 或 Deepgram 的 API Key。

**Q3：处理失败了，会议数据会丢吗？**
不会。会议和录音都还在，状态会显示 **Failed**。解决原因后（通常是 API Key 问题）
重新点 **Process Meeting** 即可。

**Q4：摘要质量一般？**
默认使用离线的"规则摘要"（不需要任何 Key，但只是简单抽取重点句子）。
想要更高质量的摘要，在 **LLM Provider** 里选择 OpenAI 或 Anthropic 并填好对应
API Key，再点一次 **Process Meeting**。

**Q5：公司里的"团队服务器"是什么？**
那是给有 IT 部署能力团队用的可选功能（多人同步、管理员审计），个人使用完全不需要。
如果您的公司开通了它，IT 会给您一个账号，登录后会议可同步到团队空间。

**Q6：换电脑后数据怎么迁移？**
把 `~/RecapData` 整个文件夹拷到新电脑的相同位置即可。API Key 需要重新填写一遍。

---

## 9. 隐私承诺（我们如何对待您的数据）

1. 录音、转写、摘要默认**只存在您自己的电脑上**；
2. 数据文件经过加密存储，密钥由您的口令派生（生产环境由 IT 统一配置口令）；
3. 只有您明确配置并点击处理时，相关音频/文本才会发送给您选择的云服务商；
4. Recap 没有任何"悄悄加入会议""后台上传"的行为。

---

## 10. 遇到问题？

- 第一步：看窗口**底部状态栏**的文字，那里有最直接的错误原因；
- 第二步：重启 Recap 再试一次；
- 第三步：联系您的 IT 支持人员，或到项目仓库提issue：
  https://github.com/OnDemandWorld/odw-recap

---

*最后更新：2026-09-11，对应版本 Recap Desktop 1.0.0*
